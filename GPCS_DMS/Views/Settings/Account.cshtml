@{
    ViewData["Title"] = "Account Settings";
}

@section Styles {
    <link rel="stylesheet" href="~/css/grid.css" asp-append-version="true" />
}

<div class="container-fluid">
    <div class="jp-minimal-header">
        <h2>Account Management</h2>
        <p>Manage your account settings efficiently using the grid below.</p>
    </div>
    <div class="grid-wrapper">
        <div id="gridAccountManagement"></div>
    </div>
</div>

@section Scripts {
    <partial name="_GridScripts" />
    <script src="~/js/grids/managements/account-management-config.js" asp-append-version="true"></script>
    
    <script>
        $(document).ready(function () {
            (async () => {
                const config = await window.createAccountManagementGridConfig();
                await GridFactory.createGrid(config);
            })();
        });
    </script>
}